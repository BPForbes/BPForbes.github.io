# Simulator tests

These tests cover protocol compilation, circuit execution, correction flows,
particle tracking, and natural-language parsing. Keeping them in one directory
separates runtime simulator modules from their regression coverage.
