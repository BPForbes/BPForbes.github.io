# Gate tests

These tests focus on gate metadata, arity validation, built-in gate behavior,
and custom gate execution. They are nested under `gates` because they validate
the registry and gate-definition layer rather than full protocol workflows.
